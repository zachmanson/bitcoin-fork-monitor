---
phase: 01-data-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - app/analytics.py
  - tests/test_analytics.py
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "calculate_stale_rate(99, 1) returns 0.01 — denominator is canonical + orphaned (100), not orphaned alone (1) or canonical alone (99)"
    - "calculate_stale_rate(0, 0) returns 0.0 without raising ZeroDivisionError"
    - "calculate_stale_rate(0, 10) returns 1.0 — all orphaned edge case works"
    - "Passing negative counts raises ValueError — invalid inputs rejected explicitly"
    - "The test suite fails if the denominator is changed — formula is pinned, not just checked for approximate output"
  artifacts:
    - path: "app/analytics.py"
      provides: "calculate_stale_rate(canonical, orphaned) pure function"
      exports: ["calculate_stale_rate"]
    - path: "tests/test_analytics.py"
      provides: "Unit tests pinning the stale rate formula denominator"
      contains: "test_stale_rate_denominator_definition"
  key_links:
    - from: "tests/test_analytics.py"
      to: "app/analytics.py"
      via: "from app.analytics import calculate_stale_rate"
      pattern: "from app.analytics import"
---

<objective>
Define the stale rate formula as a pure, tested Python function in the data layer. The test suite pins the denominator definition so that any change to the formula immediately breaks CI.

Purpose: The stale rate is the core metric of this entire project. Locking the formula with a test that explicitly checks the denominator (not just a numeric result) means the formula cannot drift silently.
Output: app/analytics.py with calculate_stale_rate(), tests/test_analytics.py with tests that fail if the denominator is changed.
</objective>

<execution_context>
@C:/Users/zachm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zachm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<feature>
  <name>calculate_stale_rate</name>
  <files>app/analytics.py, tests/test_analytics.py</files>
  <behavior>
    Formula: orphaned / (canonical + orphaned)

    Cases:
    - calculate_stale_rate(99, 1) == pytest.approx(1/100)        # 0.01 — normal case
    - calculate_stale_rate(95, 5) == pytest.approx(5/100)        # 0.05 — denominator pinned
    - calculate_stale_rate(95, 5) != pytest.approx(5/95)         # NOT orphaned/canonical
    - calculate_stale_rate(0, 0) == 0.0                          # zero-blocks edge case, no ZeroDivisionError
    - calculate_stale_rate(0, 10) == pytest.approx(1.0)          # all orphaned
    - calculate_stale_rate(100, 0) == pytest.approx(0.0)         # all canonical
    - calculate_stale_rate(-1, 5) raises ValueError              # negative input rejected
    - calculate_stale_rate(5, -1) raises ValueError              # negative input rejected
  </behavior>
  <implementation>
    Pure function in app/analytics.py. No database imports. No side effects. Returns float.
    Guard: if total == 0, return 0.0. Guard: if either count < 0, raise ValueError.
    Full Google-style docstring: what, args, returns, raises.
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests first (RED), then implement (GREEN)</name>
  <files>tests/test_analytics.py, app/analytics.py</files>
  <action>
    RED phase — write tests/test_analytics.py first. The file app/analytics.py does not exist yet, so all imports will fail (that is the expected failing state).

    Write tests/test_analytics.py with these test functions:

    test_stale_rate_normal_case():
    - assert calculate_stale_rate(canonical=99, orphaned=1) == pytest.approx(1 / 100)
    - Comment: "1 orphan in 100 total blocks = 1% stale rate"

    test_stale_rate_denominator_definition():
    - This is the formula-pinning test. It MUST fail if denominator changes.
    - canonical = 95, orphaned = 5
    - result = calculate_stale_rate(canonical, orphaned)
    - expected = orphaned / (canonical + orphaned)   # the correct formula
    - assert result == pytest.approx(expected)
    - assert result != pytest.approx(orphaned / canonical)  # explicitly NOT orphaned/canonical
    - Comment: "If this test fails, the denominator has been changed. The formula must be orphaned / (canonical + orphaned)."

    test_stale_rate_zero_blocks():
    - assert calculate_stale_rate(canonical=0, orphaned=0) == 0.0
    - Comment: "Fresh database: no division by zero"

    test_stale_rate_all_orphaned():
    - assert calculate_stale_rate(canonical=0, orphaned=10) == pytest.approx(1.0)
    - Comment: "Degenerate case: every block is orphaned"

    test_stale_rate_all_canonical():
    - assert calculate_stale_rate(canonical=100, orphaned=0) == pytest.approx(0.0)
    - Comment: "No forks ever: stale rate is zero"

    test_stale_rate_negative_canonical_raises():
    - with pytest.raises(ValueError): calculate_stale_rate(canonical=-1, orphaned=5)

    test_stale_rate_negative_orphaned_raises():
    - with pytest.raises(ValueError): calculate_stale_rate(canonical=5, orphaned=-1)

    Confirm RED: run pytest tests/test_analytics.py — expect ImportError or ModuleNotFoundError (app/analytics.py does not exist). Commit: "test(01-02): add failing stale rate formula tests"

    GREEN phase — create app/analytics.py:
    - Module-level docstring explaining this module holds data-layer business logic (not endpoints, not DB queries)
    - calculate_stale_rate(canonical: int, orphaned: int) -> float
    - Full docstring: formula explanation, args, returns, raises
    - Raise ValueError if canonical < 0 or orphaned < 0
    - total = canonical + orphaned; if total == 0: return 0.0
    - return orphaned / total

    Confirm GREEN: run pytest tests/test_analytics.py — all tests must pass. Commit: "feat(01-02): implement calculate_stale_rate formula"

    Per CLAUDE.md: small focused function, clear name, docstring explaining reasoning. No additional helper functions needed — the formula is one expression.
  </action>
  <verify>
    <automated>cd /c/Users/zachm/Documents/vibing/bitcoin-fork-monitor && pytest tests/test_analytics.py -v</automated>
  </verify>
  <done>
    - pytest tests/test_analytics.py exits 0 with all 7 tests passing
    - test_stale_rate_denominator_definition explicitly asserts result != orphaned/canonical
    - app/analytics.py contains no imports from app.models or app.database (pure function, independently testable)
    - Two commits made: one RED (failing tests), one GREEN (implementation)
  </done>
</task>

</tasks>

<verification>
Run the full test suite after both plans in this phase complete:

```
pytest tests/ -v
```

Expected: all tests in test_analytics.py, test_database.py, test_models.py pass. Zero failures, zero errors.

Denominator formula spot-check (confirms the test actually pins the formula):
Temporarily change `orphaned / total` to `orphaned / canonical` in analytics.py and run:
```
pytest tests/test_analytics.py::test_stale_rate_denominator_definition -v
```
Expected: test FAILS. Revert the change. This confirms the test is a real guard, not a tautology.
</verification>

<success_criteria>
- pytest tests/test_analytics.py exits 0 with all 7 tests passing
- app/analytics.py is a pure function module with no database imports
- The denominator test explicitly asserts the formula is NOT orphaned/canonical
- Changing the denominator in analytics.py causes test_stale_rate_denominator_definition to fail
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-02-SUMMARY.md` summarizing:
- What was built and key decisions made
- Exact file paths created
- Any deviations from this plan and why
- TDD cycle outcome (RED commit hash, GREEN commit hash)
</output>
