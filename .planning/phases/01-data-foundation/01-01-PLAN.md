---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/__init__.py
  - app/models.py
  - app/database.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_database.py
  - tests/test_models.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "Running create_db_and_tables() creates bitcoin_fork.db with three tables: block, forkevent, syncstate"
    - "Running create_db_and_tables() twice raises no error and produces no schema drift"
    - "Inserting two Block rows at the same height with different hashes both persist — no collision, no overwrite"
    - "Deleting bitcoin_fork.db and running create_db_and_tables() again recreates all three tables correctly"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project definition with uv dependencies and pytest config"
      contains: "[tool.pytest.ini_options]"
    - path: "app/models.py"
      provides: "SQLModel table definitions: Block, ForkEvent, SyncState"
      exports: ["Block", "ForkEvent", "SyncState"]
    - path: "app/database.py"
      provides: "Engine creation and create_db_and_tables function"
      exports: ["engine", "create_db_and_tables", "get_session"]
    - path: "tests/conftest.py"
      provides: "In-memory SQLite session fixture for all tests"
      contains: "StaticPool"
  key_links:
    - from: "app/database.py"
      to: "app/models.py"
      via: "import before create_all() to populate SQLModel.metadata"
      pattern: "from app.models import"
    - from: "tests/conftest.py"
      to: "app/models.py"
      via: "SQLModel.metadata.create_all(engine) in fixture"
      pattern: "create_all"
---

<objective>
Set up the Python project and define the SQLite schema as SQLModel table classes. This plan creates the data layer foundation — models, engine configuration, and test infrastructure — that all future phases import and build on.

Purpose: Every other phase reads from and writes to the tables defined here. Getting the schema right in Phase 1 (especially block hash as primary key) prevents silent data loss in later phases.
Output: Working Python project with three database tables (block, fork_events, sync_state), idempotent schema initialization, and tests confirming both persistence and the hash-as-primary-key constraint.
</objective>

<execution_context>
@C:/Users/zachm/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zachm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
</context>

<interfaces>
<!-- Key patterns established by this plan. Downstream plans import from these modules. -->

From app/models.py (created by this plan):
```python
class Block(SQLModel, table=True):
    hash: str = Field(primary_key=True)
    height: int = Field(index=True)
    timestamp: datetime
    is_canonical: bool = Field(default=True)

class ForkEvent(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    height: int
    canonical_hash: str
    orphaned_hash: str
    detected_at: datetime
    resolution_seconds: Optional[float] = Field(default=None)

class SyncState(SQLModel, table=True):
    id: Optional[int] = Field(default=None, primary_key=True)
    last_synced_height: int = Field(default=0)
    backfill_complete: bool = Field(default=False)
    updated_at: datetime
```

From app/database.py (created by this plan):
```python
engine: Engine  # SQLite engine, file-backed in production
def create_db_and_tables() -> None: ...  # Idempotent, call on startup after importing models
def get_session() -> Generator[Session, None, None]: ...  # FastAPI dependency
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project setup and SQLModel schema definition</name>
  <files>
    pyproject.toml,
    app/__init__.py,
    app/models.py,
    app/database.py,
    tests/__init__.py,
    tests/conftest.py
  </files>
  <behavior>
    - Block table uses hash (str) as primary key — inserting two rows at the same height succeeds without collision
    - ForkEvent table has canonical_hash and orphaned_hash as plain string columns (no FK enforcement — SQLite requires PRAGMA foreign_keys=ON which is off by default; document this in a comment)
    - SyncState table tracks backfill progress with last_synced_height and backfill_complete flag
    - create_db_and_tables() is idempotent — calling it twice raises no error
    - get_session() yields a Session and closes it on exit even if an exception occurs
    - conftest.py session fixture uses StaticPool + in-memory SQLite — each test gets a clean database
  </behavior>
  <action>
    Initialize the project using uv. From the repo root:

    ```
    uv init --no-workspace .
    uv add sqlmodel fastapi
    uv add --dev pytest
    ```

    If uv is not available, create pyproject.toml manually and use pip install.

    pyproject.toml must include:
    - [project] with name = "bitcoin-fork-monitor", requires-python = ">=3.12"
    - [tool.pytest.ini_options] with testpaths = ["tests"] and addopts = "-x -q"

    Create app/__init__.py as an empty file (marks app as a Python package).

    Create app/models.py with three SQLModel table classes:
    - Block: hash (str, primary_key=True), height (int, index=True), timestamp (datetime), is_canonical (bool, default=True)
    - ForkEvent: id (Optional[int], primary_key=True, default=None), height (int), canonical_hash (str), orphaned_hash (str), detected_at (datetime, default_factory=datetime.utcnow), resolution_seconds (Optional[float], default=None)
    - SyncState: id (Optional[int], primary_key=True, default=None), last_synced_height (int, default=0), backfill_complete (bool, default=False), updated_at (datetime, default_factory=datetime.utcnow)

    All three classes need table=True. All fields need docstrings on the class explaining purpose.
    Add a module-level docstring explaining that ForkEvent stores hashes as plain strings (not FK constraints) because SQLite does not enforce FK constraints by default; enforcement can be added later via PRAGMA.

    Create app/database.py:
    - DATABASE_URL = "sqlite:///bitcoin_fork.db" (file-backed, survives restarts)
    - engine = create_engine(DATABASE_URL, connect_args={"check_same_thread": False})
    - create_db_and_tables() imports models first (from app import models  # noqa: F401) then calls SQLModel.metadata.create_all(engine)
    - get_session() yields Session(engine) using a context manager

    Create tests/__init__.py as an empty file.

    Create tests/conftest.py with a session_fixture using:
    - create_engine("sqlite://", connect_args={"check_same_thread": False}, poolclass=StaticPool)
    - SQLModel.metadata.create_all(engine) inside the fixture
    - yield Session(engine)

    CLAUDE.md requires docstrings on all important functions. Add docstrings explaining what, inputs, outputs, assumptions for create_db_and_tables, get_session, and the conftest fixture.

    Per CLAUDE.md: clean, readable, modular. No inline comments explaining syntax — add comments only where reasoning is non-obvious (e.g., why check_same_thread=False is needed).
  </action>
  <verify>
    <automated>cd /c/Users/zachm/Documents/vibing/bitcoin-fork-monitor && python -c "from app.database import create_db_and_tables; create_db_and_tables(); create_db_and_tables(); print('idempotent ok')"</automated>
  </verify>
  <done>
    - pyproject.toml exists with pytest config
    - app/models.py defines Block, ForkEvent, SyncState with table=True
    - app/database.py exports engine, create_db_and_tables, get_session
    - tests/conftest.py provides in-memory session fixture using StaticPool
    - Calling create_db_and_tables() twice raises no error
    - bitcoin_fork.db created on first call, not recreated on second
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Database and model tests (DATA-01 and DATA-02)</name>
  <files>
    tests/test_database.py,
    tests/test_models.py
  </files>
  <behavior>
    - test_database.py: create_db_and_tables() creates all three tables; running it twice raises no exception; sqlite3 inspection confirms table names match model class names
    - test_models.py: inserting two Block rows at the same height (height=800000) with different hashes both survive; querying by height returns two rows; querying by hash returns exactly one row per hash
    - test_models.py: inserting a Block with a duplicate hash raises an IntegrityError (primary key violation)
    - All tests use the in-memory session fixture from conftest.py — no real db file written during tests
  </behavior>
  <action>
    Write tests/test_database.py to cover DATA-01:
    - test_create_tables_creates_all_three(): call create_db_and_tables() against an in-memory engine; use SQLAlchemy inspect() to get table names; assert "block", "forkevent", "syncstate" all present
    - test_create_tables_is_idempotent(): call create_db_and_tables() twice on the same engine; assert no exception raised
    - Use a dedicated in-memory engine inside these tests (not the conftest session fixture) so the test covers create_db_and_tables() directly

    Write tests/test_models.py to cover DATA-02:
    - test_two_blocks_same_height(): create two Block instances with height=800000 but different hashes; add both to session; commit; query by height; assert len(results) == 2
    - test_block_hash_is_primary_key(): create one Block; commit; create second Block with same hash but different height; add and attempt to commit; assert IntegrityError is raised
    - Use the session fixture from conftest.py

    Per CLAUDE.md learning environment: add a brief comment above each test function explaining WHAT behavior it is proving and WHY it matters (one line each).
  </action>
  <verify>
    <automated>cd /c/Users/zachm/Documents/vibing/bitcoin-fork-monitor && pytest tests/test_database.py tests/test_models.py -x -q</automated>
  </verify>
  <done>
    - pytest tests/test_database.py passes (DATA-01 verified)
    - pytest tests/test_models.py passes (DATA-02 verified)
    - All tests use in-memory SQLite — no bitcoin_fork.db written during test runs
  </done>
</task>

</tasks>

<verification>
Full plan verification after both tasks complete:

```
pytest tests/test_database.py tests/test_models.py -v
```

Expected: all tests green. No warnings about missing tables or import errors.

Schema spot-check (optional, confirms production engine):
```
python -c "
from app.database import create_db_and_tables
import sqlite3
create_db_and_tables()
conn = sqlite3.connect('bitcoin_fork.db')
tables = conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()
print([t[0] for t in tables])
"
```
Expected output includes: `block`, `forkevent`, `syncstate`
</verification>

<success_criteria>
- pytest tests/test_database.py tests/test_models.py exits 0 with all tests passing
- app/models.py, app/database.py exist and import without errors
- Block.hash is the primary key — confirmed by IntegrityError test on duplicate hash
- create_db_and_tables() is idempotent — confirmed by double-call test
- No bitcoin_fork.db written during test runs (tests use in-memory SQLite)
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` summarizing:
- What was built and key decisions made
- Exact file paths created
- Any deviations from this plan and why
- Patterns established for future phases to follow
</output>
